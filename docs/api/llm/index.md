# sage_sanctum.llm

LLM model categories, references, selection, and gateway chat model.

## Modules

| Module | Description |
|--------|-------------|
| [`model_category`](model_category.md) | `ModelCategory` enum (TRIAGE, ANALYSIS, REASONING, EMBEDDINGS) |
| [`model_ref`](model_ref.md) | `ModelRef` — canonical `provider:model` references |
| [`model_selector`](model_selector.md) | `ModelSelector` and `StaticModelSelector` |
| [`gateway_chat`](gateway_chat.md) | `GatewayChatModel` — LangChain chat model via gateway |
