# sage_sanctum.gateway.client

Gateway client abstract base class with `SpiffeGatewayClient` (production) and `DirectProviderClient` (local development) implementations.

::: sage_sanctum.gateway.client
